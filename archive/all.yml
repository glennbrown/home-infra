---

# grog.authorized-key
authorized_key_list:
  - name: "{{ main_username }}"
    authorized_keys:
      - key: "{{ secret_gbrown_sshkey }}"
  - name: root
    authorized_keys:
      - key: "{{ secret_root_sshkey }}"