---
- name: Load or remove kernel modules
  community.general.modprobe:
    name: "{{ item.name }}"
    state: "{{ item.state | default('present') }}"
    persistent: "{{ item.state | default('present') }}"
  loop: "{{ kernel_modules }}"
  when: kernel_modules is defined and kernel_modules | length > 0
